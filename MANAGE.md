# راهنمای مدیریت سایت SysaiQ (بدون دست‌زدن به کد)

سایت روی GitHub Pages میزبانی می‌شود و **هر تغییری که روی شاخه‌ی main پوش
شود، خودکار بیلد و منتشر می‌شود** (ورک‌فلوی `.github/workflows/deploy.yml`).
یعنی «بک‌اند مدیریت» شما همین ریپوی گیت‌هاب است:

## ویرایش متن‌های سایت (فارسی و انگلیسی)

1. به این فایل بروید:
   https://github.com/Hamedab1987/sysaiq-web/blob/main/vesper-project/src/content.py
2. آیکون مداد (Edit) را بزنید.
3. متن موردنظر را عوض کنید — هر متن دو نسخه دارد: `"en"` و `"fa"`.
4. پایین صفحه **Commit changes** را بزنید.
5. یکی دو دقیقه بعد، sysaiq.com به‌روز می‌شود. (تب Actions روند بیلد را نشان می‌دهد.)

نکته: متن پروژه‌های اسلایدر با کلیدهای `W1_T` تا `W5_G` مشخص شده‌اند
(T=عنوان، D=توضیح، G=تگ‌ها).

## عوض کردن لوگو

فایل `vesper-project/assets/logo-src.png` را با نسخه‌ی جدید جایگزین کنید و
به Claude بگویید نسخه‌های مشتق (favicon، هدر، og) را دوباره بسازد.

## قواعد مهم

- فایل‌های `en/index.html`، `fa/index.html` و `index.html` ریشه **خروجی
  بیلد هستند — هرگز مستقیم ویرایش نشوند.**
- متن فارسی را طبیعی بنویسید؛ اصطلاحات فنی (AI، Agent، RAG، …) انگلیسی
  می‌مانند.
- اگر توکنی از قالب حذف/اضافه شود بیلد عمداً خطا می‌دهد تا دو زبان از هم
  جدا نیفتند.

## آدرس‌ها

- سایت: https://sysaiq.com (فارسی: /fa/ — انگلیسی: /en/)
- ریپو: https://github.com/Hamedab1987/sysaiq-web
- وضعیت دیپلوی: https://github.com/Hamedab1987/sysaiq-web/actions
